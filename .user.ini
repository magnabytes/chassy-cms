; Raise upload limits for PHP-FPM deployments (nginx, non-cPanel hosts).
; On Apache + mod_php (cPanel shared hosting), .htaccess handles this instead.
; On local dev with php -S, use: php -c dev-php.ini -S localhost:8080

upload_max_filesize = 10M
post_max_size = 16M
